export type ProfileAddress = {
    address_name: string;
    address: string;
    number: string;
    complement?: string;
    district: string;
    zip_code: string;
    city: string;
    state: string;
}